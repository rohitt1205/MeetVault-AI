{
  "name": "meetvault-ai-root",
  "version": "1.0.0",
  "description": "Root package for running the frontend and backend",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "install:all": "npm install --prefix frontend"
  }
}
